services:
  app:
    build:
      context: .
    image: nippogen-backend:latest
    container_name: nippogen-backend
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:17
    container_name: nippogen-postgres
    environment:
      POSTGRES_DB: nippogen
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432" # localのpostgresとの競合を防ぐため

  # MCPサーバーは後で追加予定
  # GitHub MCP Server
  # mcp-github:
  #   image: node:18-alpine
  #   container_name: nippogen-mcp-github
  #   working_dir: /app
  #   command: sh -c "npm install -g @modelcontextprotocol/server-github && mcp-github-server"
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     - GITHUB_TOKEN=${GITHUB_TOKEN}

volumes:
  db-data:
